---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import TypewriterHeadline from '../components/TypewriterHeadline';
import InteractiveQuizCard from '../components/InteractiveQuizCard';
import QTMascot from '../components/QTMascot';
import ClientMarquee from '../components/ClientMarquee.astro';
import QShalaEffectTabs from '../components/QShalaEffectTabs';
import InteractiveCaseStudiesShowcase from '../components/InteractiveCaseStudiesShowcase';
import AnimatedHeroMascotStage from '../components/AnimatedHeroMascotStage';
import RollingStatCounters from '../components/RollingStatCounters';
import FAQSection from '../components/FAQSection';
import TestimonialsMarquee from '../components/TestimonialsMarquee.astro';
import { FEATURED_QUIZZES, CASE_STUDIES_DATA, PRODUCTS_DATA } from '../lib/data';

const UPCOMING_EVENTS = [
  {
    type: "Offline Event",
    typeBg: "bg-rose-100",
    typeText: "text-rose-700",
    date: "Oct 15, 2026",
    title: "Bengaluru Grand Quiz Fest",
    desc: "A massive city-wide trivia showdown for all age groups. Bring your family!",
    ctaText: "Register Now",
    btnBg: "bg-[#30B2E7]",
    btnText: "text-white",
    hoverColor: "hover:bg-[#209dd1]",
    hoverTextColor: "group-hover:text-[#30B2E7]",
    mascot: "happy"
  },
  {
    type: "Online Quiz",
    typeBg: "bg-amber-100",
    typeText: "text-amber-700",
    date: "Oct 22, 2026",
    title: "The Tech History Showdown",
    desc: "Test your knowledge on the evolution of technology, from the abacus to AI.",
    ctaText: "Join Waitlist",
    btnBg: "bg-[#FDB913]",
    btnText: "text-slate-900",
    hoverColor: "hover:bg-amber-400",
    hoverTextColor: "group-hover:text-[#FDB913]",
    mascot: "sherlock"
  },
  {
    type: "Corporate League",
    typeBg: "bg-sky-100",
    typeText: "text-sky-700",
    date: "Nov 05, 2026",
    title: "QShala Corporate Clash",
    desc: "Compete against other top companies in this team-building trivia tournament.",
    ctaText: "Register Team",
    btnBg: "bg-[#75B543]",
    btnText: "text-white",
    hoverColor: "hover:bg-[#5f9c31]",
    hoverTextColor: "group-hover:text-[#75B543]",
    mascot: "quizzing"
  },
  {
    type: "School Championship",
    typeBg: "bg-green-100",
    typeText: "text-green-700",
    date: "Dec 10, 2026",
    title: "National Curiosity Cup",
    desc: "The grand finale of our nation-wide inter-school quiz competition.",
    ctaText: "Learn More",
    btnBg: "bg-[#9333EA]",
    btnText: "text-white",
    hoverColor: "hover:bg-purple-700",
    hoverTextColor: "group-hover:text-[#9333EA]",
    mascot: "thinking"
  }
];
---

<BaseLayout title="QShala | Questions • Quriosity • Quests — India's Quriosity Company">
  <Navbar />

  <main class="flex-grow overflow-x-hidden">
    
    <!-- 1. HERO (Banner) -->
    <section id="hero" class="relative pt-28 pb-16 md:pt-32 md:pb-20 bg-[#FFFDF5]">
      
      <!-- Ambient Glowing Orbs -->
      <div class="absolute top-10 left-1/4 w-96 h-96 bg-[#30B2E7]/15 rounded-full blur-3xl pointer-events-none animate-pulse"></div>
      <div class="absolute top-40 right-1/4 w-[28rem] h-[28rem] bg-[#FDB913]/20 rounded-full blur-3xl pointer-events-none animate-pulse" style={{ animationDelay: '1s' }}></div>

      <div class="max-w-7xl mx-auto px-6 relative z-10">
        
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
          
          <!-- Left Content -->
          <div class="lg:col-span-7 space-y-6 text-left">
            <div class="inline-flex items-center gap-2.5 px-4 py-2 rounded-full bg-white border border-slate-200 shadow-sm">
              <span class="w-2.5 h-2.5 rounded-full bg-[#30B2E7] animate-ping"></span>
              <span class="text-xs font-black uppercase text-slate-800 tracking-wider font-heading">
                India&apos;s Quriosity Platform
              </span>
            </div>

            <div class="text-left">
              <TypewriterHeadline client:idle />
            </div>

            <p class="text-lg sm:text-xl font-semibold text-slate-700 max-w-3xl leading-relaxed pt-2">
              QShala replaces rote memorization across schools, colleges, and modern workplaces with socratic storytelling, interactive trivia, and active curiosity that empower minds to ask <span class="text-[#75B543] font-black underline decoration-[#75B543]/40 decoration-4">&ldquo;Why?&rdquo;</span>
            </p>

            <div class="pt-2 flex flex-wrap items-center gap-4">
              <a
                href="/book-a-quiz"
                class="px-8 py-4 rounded-full bg-[#FDB913] hover:bg-amber-400 text-black font-black text-base shadow-md hover:-translate-y-0.5 transition-all flex items-center gap-2 font-heading"
              >
                Book a Quriosity Experience →
              </a>
            </div>
          </div>

          <!-- Right Interactive Stage (Trivia Generator) -->
          <div class="lg:col-span-5 flex justify-center w-full">
            <AnimatedHeroMascotStage client:visible />
          </div>

        </div>

      </div>
    </section>

    <!-- 2. UPCOMING EVENTS -->
    <section id="events" class="py-20 bg-[#E8F6FD] border-t border-b border-sky-100">
      <div class="max-w-7xl mx-auto px-6">
        <div class="text-center mb-12 flex flex-col items-center">
          <div class="inline-flex items-center justify-center gap-4 mb-4">
            <h2 class="text-4xl md:text-5xl font-black text-slate-900 font-heading">Upcoming Events</h2>
            <div class="shrink-0">
               <QTMascot client:visible variant="happy" size="md" />
            </div>
          </div>
          <p class="text-slate-600 font-semibold">Join us for live quizzes, offline tournaments, and community events.</p>
        </div>
        
        <div class="marquee-container relative w-full overflow-hidden">
          <div class="pointer-events-none absolute left-0 top-0 bottom-0 w-8 md:w-24 z-10 bg-gradient-to-r from-[#E8F6FD] to-transparent"></div>
          <div class="pointer-events-none absolute right-0 top-0 bottom-0 w-8 md:w-24 z-10 bg-gradient-to-l from-[#E8F6FD] to-transparent"></div>
          
          <div class="marquee-track flex gap-6 w-max px-4">
            {[...UPCOMING_EVENTS, ...UPCOMING_EVENTS, ...UPCOMING_EVENTS].map((ev) => (
              <div class="w-[300px] md:w-[380px] bg-white rounded-3xl p-6 shadow-sm border border-sky-50 hover:shadow-md transition-shadow group flex flex-col justify-between shrink-0 relative overflow-hidden cursor-pointer">
                
                <div class="relative z-10">
                  <div class="flex items-start justify-between mb-4">
                    <div class="flex items-center gap-3 flex-wrap">
                      <span class={`px-3 py-1 ${ev.typeBg} ${ev.typeText} font-black text-[10px] uppercase font-heading rounded-full`}>{ev.type}</span>
                      <span class="text-xs font-bold text-slate-500">{ev.date}</span>
                    </div>
                    <div class="shrink-0">
                      <QTMascot client:visible variant={ev.mascot as any} size="sm" />
                    </div>
                  </div>
                  <h3 class={`text-xl md:text-2xl font-black text-slate-900 font-heading mb-2 ${ev.hoverTextColor} transition-colors`}>{ev.title}</h3>
                  <p class="text-slate-600 text-sm font-medium mb-6 pr-4">{ev.desc}</p>
                </div>
                <button class={`w-full py-3.5 rounded-full ${ev.btnBg} ${ev.btnText} font-black text-xs uppercase font-heading ${ev.hoverColor} transition-colors relative z-10 shadow-sm`}>
                  {ev.ctaText} &rarr;
                </button>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- 3. TRUSTED BY THE BEST -->
    <section id="partners" class="py-16 bg-white">
      <ClientMarquee />
    </section>

    <!-- 4. EXPLORE THE QSHALA EFFECT -->
    <section id="effect" class="relative overflow-hidden">
      <QShalaEffectTabs client:visible />
    </section>

    <!-- 5. GAME (Daily Quiz Playground) -->
    <section id="game" class="py-16 md:py-24 bg-[#FFFDF5]">
      <div class="max-w-7xl mx-auto px-6">
        <div class="text-center max-w-xl mx-auto mb-12">
          <span class="px-4 py-1.5 rounded-full bg-[#75B543]/15 text-[#75B543] font-black text-xs uppercase font-heading">
            Daily Game
          </span>
          <h3 class="text-4xl font-black text-slate-900 mt-4 font-heading">Test Your Quriosity</h3>
          <p class="text-sm font-semibold text-slate-600 mt-2">Pick an answer below to trigger a celebration!</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 items-stretch">
          {FEATURED_QUIZZES.map((quiz) => (
            <InteractiveQuizCard client:load quiz={quiz} />
          ))}
        </div>
      </div>
    </section>

    <!-- 6. NEWSLETTER / QSHOTS -->
    <section id="newsletter" class="py-16 md:py-20 bg-[#F2FCE8] relative overflow-hidden border-t border-[#75B543]/20">
      <div class="max-w-7xl mx-auto px-6 flex flex-col lg:flex-row items-center justify-between gap-12 relative z-10">
        
        <!-- Left Content -->
        <div class="text-center lg:text-left space-y-4 max-w-xl">
          <span class="px-3.5 py-1 rounded-full bg-[#75B543] text-white font-black text-[10px] uppercase tracking-wider font-heading inline-block">
            Newsletter
          </span>
          <h2 class="text-4xl md:text-5xl font-black text-slate-900 tracking-tight font-heading">
            Subscribe to QShots.
          </h2>
          <p class="text-slate-700 text-base md:text-lg font-semibold">
            Get weekly trivia bites straight to your inbox. No spam, just pure curiosity.
          </p>
        </div>
        
        <!-- Right Form & Mascot -->
        <div class="w-full lg:w-1/2 flex flex-col sm:flex-row items-center justify-center lg:justify-end gap-6">
          <form class="flex w-full max-w-md flex-col sm:flex-row gap-2 relative z-10" onsubmit="event.preventDefault();">
            <input 
              type="email" 
              placeholder="Enter your email address" 
              class="flex-grow px-6 py-4 rounded-full bg-white border border-slate-200 text-sm font-medium focus:outline-none focus:border-[#75B543] focus:ring-4 focus:ring-[#75B543]/10 shadow-sm transition-all"
              required
            />
            <button 
              type="submit" 
              class="px-8 py-4 rounded-full bg-slate-900 text-white font-black text-xs uppercase font-heading hover:bg-[#75B543] transition-colors whitespace-nowrap shadow-md hover:shadow-lg hover:-translate-y-0.5"
            >
              Subscribe &rarr;
            </button>
          </form>

          <!-- Mascot -->
          <div class="hidden sm:block shrink-0 transform hover:scale-110 hover:-rotate-6 transition-transform duration-300">
             <QTMascot client:visible variant="happy" size="md" />
          </div>
        </div>

      </div>
    </section>

    <!-- 7. CASE STUDIES -->
    <section id="case-studies" class="py-16 md:py-24 bg-white">
      <div class="max-w-7xl mx-auto px-6 space-y-10">
        <div class="flex flex-col md:flex-row md:items-end justify-between">
          <div>
            <span class="px-4 py-1.5 rounded-full bg-[#30B2E7] text-white font-black text-xs uppercase tracking-wider font-heading">
              Real Impact Stories
            </span>
            <h2 class="text-4xl md:text-5xl font-black text-slate-900 tracking-tight mt-4 font-heading">
              10+ Storytelling Case Studies.
            </h2>
          </div>
          <a href="/case-studies" class="mt-4 md:mt-0 px-6 py-3 rounded-full bg-[#30B2E7] hover:bg-sky-500 text-white font-black text-xs border-2 border-black shadow-[3px_3px_0px_0px_rgba(0,0,0,1)] font-heading transition-all inline-block">
            Explore All 10 Case Studies →
          </a>
        </div>

        <InteractiveCaseStudiesShowcase caseStudies={CASE_STUDIES_DATA} client:visible />
      </div>
    </section>

    <!-- 8. WALL OF LOVE -->
    <div class="bg-[#FFFDF5] pt-12 pb-24">
      <TestimonialsMarquee />
    </div>

    <!-- 9. FREQUENTLY ASKED QUESTIONS -->
    <section id="faq" class="py-12 md:py-16 bg-[#FFFDF5] relative">
      <div class="max-w-7xl mx-auto px-6">
        <div class="text-center max-w-2xl mx-auto mb-14 space-y-2">
          <span class="px-4 py-1.5 rounded-full bg-[#75B543] text-white font-black text-xs uppercase tracking-wider font-heading">
            Need Clarification?
          </span>
          <h2 class="text-4xl font-black text-slate-900 font-heading">Frequently Asked Questions</h2>
          <p class="text-slate-600 text-sm font-semibold">Everything you need to know about QShala sessions and methodology.</p>
        </div>
        <FAQSection client:load />
      </div>
    </section>

    <style>
      .marquee-track {
        animation: scroll-left-events 40s linear infinite;
      }
      .marquee-container:hover .marquee-track {
        animation-play-state: paused;
      }
      @keyframes scroll-left-events {
        0% { transform: translateX(0); }
        100% { transform: translateX(calc(-100% / 3)); }
      }
    </style>
  </main>

  <Footer />
</BaseLayout>
